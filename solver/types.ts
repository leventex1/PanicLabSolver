

export enum CardType {
    STARTER,
    FIGURE,
    TRAPDOOR,
    CHANGER 
}

export enum FigureType {
    ONEEYED,
    TOWEYED
}

export enum FigureColor {
    RED,
    BLUE
}

export enum FigureTexture {
    STRIPED,
    DOTTED
}

export enum ChangerType {
    FIGURETYPE,
    FIGURECOLOR,
    FIGURETEXTURE
}

export enum StarterType {
    BLUE,
    RED,
    YELLOW
}
